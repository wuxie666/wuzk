<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wuzk.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.wuzk.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectUserById" resultMap="userResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectAllUsers" resultMap="userResultMap">
        SELECT * FROM user ORDER BY update_time DESC
    </select>

    <insert id="insertUser" parameterType="com.wuzk.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, dept_id, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{deptId}, now(), now())
    </insert>

    <update id="updateUser" parameterType="com.wuzk.entity.User">
        UPDATE user
        SET username = #{username},
            password = #{password},
            email = #{email},
            dept_id = #{deptId},
            update_time = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteUserById" parameterType="long">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="selectUserByUsername" parameterType="string" resultMap="userResultMap">
        SELECT id, username, password, email, dept_id, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

</mapper>
